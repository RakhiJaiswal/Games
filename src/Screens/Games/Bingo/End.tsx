import React from 'react';
import {View, Text} from 'react-native';

const End = ({winner}) => {
  return (
    <View>
      <Text> winner is : {winner} </Text>
    </View>
  );
};

export default End;
